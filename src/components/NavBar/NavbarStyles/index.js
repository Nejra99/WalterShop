import { styled } from '@mui/system';
import { Box } from '@mui/material';

export const CartBox = styled(Box, { name: 'cartbox' })({
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'center',
});
