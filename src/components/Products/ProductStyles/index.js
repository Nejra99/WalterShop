import { styled } from '@mui/system';
import { Box } from '@mui/material';

export const ImageBox = styled(Box, { name: 'imagebox' })({
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'left',
    marginLeft: '20%',
    marginTop: '8%',
});
